#include <bits/stdc++.h>
using namespace std;
#define ll long long
int main() {
    int x, y;
    double d;
    cin >> x >> y;
    d = sqrt(x* 1ll * x + y * 1ll* y);
    cout << round(d) << '\n';
    cin >> x >> y;
    d = sqrt(x* 1ll * x + y * 1ll* y);
    cout << round(d) << '\n';
    return 0;
}
 
