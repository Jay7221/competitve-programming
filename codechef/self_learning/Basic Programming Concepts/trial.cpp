#include <bits/stdc++.h>
using namespace std;
void solve(){
	int L, R;
	cin >> L >> R;
	cout << 2 * R - 2 * L + 1 << "\n";
}
int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	int T;
	cin >> T;
	while(T--){
		solve();
	}
}